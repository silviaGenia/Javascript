//for...of loop: Usado para iterar sobre elementos de objetos iterables.
//Loop "of-in" objetos iterables arrays y string

let animales = ["Vaca", "Toro", "Oveja"]; //

for (let animal of animales) {
  console.log(animal);
}
